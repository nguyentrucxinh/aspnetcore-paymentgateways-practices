@using LibraryApp.Data.ViewModels;
@model StripeDashboardVM;

@{
    ViewData["Title"] = "Index";
}

<h2>Stripe overview</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Type</th>
            <th scope="col">Status</th>
            <th scope="col">Amount</th>
            <th scope="col">Created?</th>
            <th scope="col">Available On</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var transaction in Model.Transactions)
        {
            <tr>
                <th scope="row">@transaction.Type</th>
                <th>@transaction.Status</th>
                <th>$@string.Format("{0:#.00}", Convert.ToDecimal(transaction.Amount) / 100)</th>
                <th>@transaction.Created</th>
                <th>@transaction.AvailableOn</th>
            </tr>
        }
    </tbody>
</table>

<hr />

<h2>Stripe Customers</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Type</th>
            <th scope="col">Balance</th>
            <th scope="col">Created?</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model.Customers)
        {
            <tr>
                <th scope="row">@customer.Name</th>
                <th>$@string.Format("{0:#.00}", Convert.ToDecimal(customer.Balance) / 100)</th>
                <th>@customer.Created</th>
            </tr>
        }
    </tbody>
</table>

<hr />

<h2>Stripe Charges</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Amount</th>
            @* <th scope="col">Source</th> *@
            @* <th scope="col">Receipt Email</th> *@
            <th scope="col">Receipt Number</th>
            @* <th scope="col">Receipt Url</th> *@
            <th scope="col">Created</th>
            <th scope="col">Currency</th>
            <th scope="col">CustomerId</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var charge in Model.Charges)
        {
            <tr>
                <th scope="row">$@string.Format("{0:#.00}", Convert.ToDecimal(charge.Amount) / 100)</th>
                @* <th>@charge.Source</th>
                <th>@charge.ReceiptEmail</th> *@
                <th>@charge.ReceiptNumber</th>
                @* <th>@charge.ReceiptUrl</th> *@
                <th>@charge.Created</th>
                <th>@charge.Currency</th>
                <th>@charge.CustomerId</th>
            </tr>
        }
    </tbody>
</table>

<hr />

<h2>Stripe Charges</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Amount</th>
            @* <th scope="col">Source</th> *@
            @* <th scope="col">Receipt Email</th> *@
            <th scope="col">Receipt Number</th>
            @* <th scope="col">Receipt Url</th> *@
            <th scope="col">Created</th>
            <th scope="col">Currency</th>
            <th scope="col">CustomerId</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var charge in Model.Charges)
        {
            <tr>
                <th scope="row">$@string.Format("{0:#.00}", Convert.ToDecimal(charge.Amount) / 100)</th>
                @* <th>@charge.Source</th>
                <th>@charge.ReceiptEmail</th> *@
                <th>@charge.ReceiptNumber</th>
                @* <th>@charge.ReceiptUrl</th> *@
                <th>@charge.Created</th>
                <th>@charge.Currency</th>
                <th>@charge.CustomerId</th>
            </tr>
        }
    </tbody>
</table>

<hr />

<h2>Stripe Disputes</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Amount</th>
            <th scope="col">Created</th>
            <th scope="col">Currency</th>
            <th scope="col">Reason</th>
            <th scope="col">Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var dispute in Model.Disputes)
        {
            <tr>
                <th scope="row">$@string.Format("{0:#.00}", Convert.ToDecimal(dispute.Amount) / 100)</th>
                <th>@dispute.Created</th>
                <th>@dispute.Currency</th>
                <th>@dispute.Reason</th>
                <th>@dispute.Status</th>
            </tr>
        }
    </tbody>
</table>

<hr />

<h2>Stripe Refunds</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Amount</th>
            <th scope="col">Receipt Number</th>
            <th scope="col">Created</th>
            <th scope="col">Currency</th>
            <th scope="col">Reason</th>
            <th scope="col">Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var refund in Model.Refunds)
        {
            <tr>
                <th scope="row">$@string.Format("{0:#.00}", Convert.ToDecimal(refund.Amount) / 100)</th>
                <th>@refund.ReceiptNumber</th>
                <th>@refund.Created</th>
                <th>@refund.Currency</th>
                <th>@refund.Reason</th>
                <th>@refund.Status</th>
            </tr>
        }
    </tbody>
</table>

<hr />

<h2>Stripe Products</h2>
<div class="row">
    <div class="col-md-6">
        <h4>Available Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Available.Sum(n => n.Amount)) / 100)</h4>
    </div>
    <div class="col-md-6">
        <h4>Pending Credit: $@string.Format("{0:#.00}", Convert.ToDecimal(Model.Balance.Pending.Sum(n => n.Amount)) / 100)</h4>
    </div>
</div>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Created</th>
            <th scope="col">Description</th>
            <th scope="col">Unit Label</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model.Products)
        {
            <tr>
                <th>@product.Name</th>
                <th>@product.Created</th>
                <th>@product.StatementDescriptor</th>
                <th>@product.UnitLabel</th>
            </tr>
        }
    </tbody>
</table>